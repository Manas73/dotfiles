---
# Current Linux workstation (Garuda/Arch).
# Identity / hardware deltas only. Shared recipe lives in
# group_vars/workstation_personal.yml (machine class).

primary_user: ms-garuda

# Chezmoi inventory data (host-specific).
email: "manas.sambare@gmail.com"
gpu: "nvidia"
