---
# Ansible Galaxy requirements.
# Install with:
#   ansible-galaxy install -r ansible/requirements.yml
#
# Needed for:
#   community.general.pacman / homebrew / homebrew_cask (packages role)
#   community.general.osx_defaults (macos_defaults role)

collections:
  - name: community.general

roles: []
