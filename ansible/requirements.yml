---
# Ansible Galaxy requirements for the provisioning layer.
# Install with:
#   ansible-galaxy install -r ansible/requirements.yml

collections:
  - name: community.general
  - name: ansible.posix
  - name: community.crypto

roles: []
