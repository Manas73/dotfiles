---
# Ansible Galaxy requirements.
# Install with:
#   ansible-galaxy install -r ansible/requirements.yml
#
# Needed for:
#   community.general.pacman  (provider_pacman role)

collections:
  - name: community.general

roles: []
