---
# Linux-only shared defaults.
#
# Currently empty. Arch-specific packages live in arch.yml. If a non-Arch
# Linux host ever joins the inventory, shared Linux packages can move here
# with OS-specific lists overriding in the child group.
