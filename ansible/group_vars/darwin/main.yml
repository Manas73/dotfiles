---
# OS group: darwin (hosts.yml → darwin).
# Package intent: apps.yml (os_apps). Machine recipe: recipes/<recipe>.yml.

# Default interpreter for macOS hosts; override on the host if needed.
ansible_python_interpreter: /usr/bin/python3

# Chezmoi osid for every Darwin host.
osid: "darwin"
